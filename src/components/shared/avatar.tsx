// Re-export from the canonical ui/ location.
// The shared/ duplicate was a 0-byte placeholder; use ui/avatar directly.
export { Avatar } from "@/components/ui/avatar";
export type { AvatarProps, AvatarSize, AvatarStatus } from "@/components/ui/avatar";
