// Placeholder — SupabaseProvider is not yet wired.
// When implemented, this should wrap children with a Supabase session context.
// Currently unused; kept as a stub so the import path resolves.

export function SupabaseProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
